import { ProddetailDirective } from './proddetail.directive';

describe('ProddetailDirective', () => {
  it('should create an instance', () => {
    const directive = new ProddetailDirective();
    expect(directive).toBeTruthy();
  });
});
